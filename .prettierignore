build
node_modules
example/android
example/ios/Pods

# Asset catalogs are generated and rewritten by Xcode, so formatting them creates churn
# in every build rather than enforcing a style anyone maintains by hand.
example/ios/**/Images.xcassets
