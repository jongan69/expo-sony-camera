import type { SonyCameraViewProps } from './SonyCamera.types';

// SonyCameraView is not available on the web platform.
export default function SonyCameraView(_props: SonyCameraViewProps) {
  return null;
}
